const { Client, Collection, Channel, DMChannel, GroupDMChannel, Guild, GuildChannel, GuildMember, Message, MessageReaction, PermissionOverwrites, Presence, Role, Snowflake, TextChannel, User, VoiceChannel, Webhook } = require('discord.js');

module.exports = {
  Client,
  Collection,
  Channel,
  DMChannel,
  GroupDMChannel,
  Guild,
  GuildChannel,
  GuildMember,
  Message,
  MessageReaction,
  PermissionOverwrites,
  Presence,
  Role,
  Snowflake,
  TextChannel,
  User,
  VoiceChannel,
  Webhook
};

// © 2022 @Sempai Development